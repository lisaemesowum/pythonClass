"""
i imported request 
The line import requests tells Python to load the Requests library into your code,
allowing your script 
to communicate with the internet.

# ===================================================================================
Without this line, Python cannot natively talk to web servers easily.


What It Actually Does?
Once imported, you use it to interact with websites and web services (APIs) using HTTP protocols. It acts like a web browser behind the scenes.


Fetches web pages:

It downloads HTML source code from websites.

Interacts with APIs: 

It sends and receives data (usually JSON format).

Handles authentication: 

It logs into accounts or passes API security keys.

Submits web forms:
It uploads text, files, or images to a server.
"""
